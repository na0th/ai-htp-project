name: jenkins build trigger workflow

on:
  workflow_call:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: trigger jenkins job.
      uses: appleboy/jenkins-action@master  
      with:
        url: ${{ secrets.WEBHOOK_URL }}
        user: "chaeeerish"
        token: ${{ secrets.JENKINS_API_TOKEN }}
        job: "jenkins-ai-drawing-test"